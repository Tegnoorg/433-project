//header file containing the prototypes of functions used to read the light sensor values
#include <stdlib.h>
#include <stdbool.h>
#include <stdio.h>

//returns true if force is greater than an ~1kg, false otherwise
bool hasForce(void);